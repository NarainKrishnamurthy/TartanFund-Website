<div class="container-fluid viewOffset whiteback" ng-controller="membersController">
    <h1 class="text-center">Members</h1>
    <div class="row">
        <div class="col-md-10 col-md-ofset-3 open_sans">
            <tabset>
                <tab ng-repeat="(setName, members) in membersConfig.members" 
                heading="{{setName}}">
                    <div class="col-sm-3 open_sans" ng-repeat="x in members track by $index">
                      <br/>
                      <img src="{{x.profilePicURL}}" style="width: 150px; height:120px" ng-click="openModal(x, 'memberModal')"></img><br/>
                      {{x.firstName}} {{x.lastName}}
                    </div>
                </tab>
            </tabset>
        </div>
    </div>
    <div class="modal fade" id="memberModal" role="dialog" tabindex="-1">
      <div class="modal-dialog">
         <div class="modal-content">
            <div class="modal-header">
               <h4 class="modal-title">{{displayedMember.firstName}} {{displayedMember.lastName}}</h4>
            </div>
            <div class="modal-body">
               <div class="row">
                  <div class="col-md-6">
                     <img src="{{displayedMember.profilePicURL}}" style="width: 180px; height: 140px"></img>
                  </div>
                  <div class="col-md-6">
                     Gradution Year: {{displayedMember.graduationYear}} <br/>
                     Email: {{displayedMember.email}} <br/>
                     Full-Time: {{displayedMember.fullTime || 'TBA'}} <br/>
                     Internship: {{displayedMember.internship || 'TBA'}} <br/>
                  </div>
               </div>
            </div>
         </div>
      </div>
    </div>
</div>
